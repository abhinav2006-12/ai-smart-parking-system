# Frontend

The React/Vite dashboard source is kept in the repository root (`src/`, `index.html`, `vite.config.js`) to preserve the original project layout. The production frontend container is defined in `docker/frontend.Dockerfile`.
